Table UserInfo:
	user_id int primary
	first_name varchar(20) not null
	last_name varchar(20) not null
	username varchar(100) not null
	email varchar(50)
	budget decimal(13,2)
	start_date date
	end_date date
	salt varchar(100)
	hashed_password varchar(100)
	google_user tinyint(1)
Table UserSymbol:
	symbol varchar(5) primary
	user_id int primary, foreign
	number_of_shares double
Table Symbols:
	symbol_id primary,
	symbol varchar(5),
	ipo_date date,
	delisting_date
	
//stored procedure

ADD_TO_SYMBOLS_AND_CREATE_TABLE: create new table named after symbol( e.g. GOOGL, AAPL) for each symbol added to Symbols_table

//the purpose of these is to get the date prior to the first date in table.
FIRST_DATE_IN_TABLE:
DIFF_TO_FIRST_DATE: 
PREVIOUS_TO_FIRST_DATE:

// get the date after the last date in table
LAST_DATE_IN_TABLE:
DIFF_TO_LAST_DATE:
NEXT_TO_LAST_DATE:

UPDATE_IPO_DELISTING_DATE: update ipo date and delisting date in Symbols_table

GET_MUTUAL_IPO_DELISTING_DATE: 

QUERY_DATA:

	
	
	